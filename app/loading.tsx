import React from 'react'
import Loader from '@/components/partials/loader'

const Loading = () => {
  return <Loader wrapperClassName={'h-screen'} spinnerClassName={'size-6'} />
}

export default Loading
